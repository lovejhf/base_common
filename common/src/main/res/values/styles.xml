<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="LoadingDialog">
        <!--对话框背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--边框 -->
        <item name="android:windowFrame">@null</item>
        <!--没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!--背景透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否有覆盖 -->
        <item name="android:windowContentOverlay">@null</item>
        <!--背景变暗-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style parent="Theme.AppCompat.Light.NoActionBar" name="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@color/pink</item>
        <item name="android:textAllCaps">false</item>
        <!--    <item name="android:windowBackground">@color/white95</item>-->
        <item name="android:textSize">13sp</item>
        <item name="pressedAlpha">0.8</item>
        <item name="disabledAlpha">0.2</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoDisplay">false</item>
        <!-- 搞掂字体大小-->
        <item name="android:textColor">@color/black90</item>
        <item name="actionMenuTextColor">@android:color/black</item>

    </style>
</resources>